import { PostAdatokRouting } from './post-adatok-routing';

describe('PostAdatokRouting', () => {
  it('should create an instance', () => {
    expect(new PostAdatokRouting()).toBeTruthy();
  });
});
